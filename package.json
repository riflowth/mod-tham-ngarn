{
  "name": "mod-tham-ngarn",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "lerna run dev --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "engines": {
    "node": "16.15.0"
  }
}
